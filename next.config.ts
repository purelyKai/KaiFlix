import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  typescript: {
    ignoreBuildErrors: true, // FIX THIS: Only use this temporarily for debugging
  },
};

export default nextConfig;
